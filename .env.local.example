# Copy this file to .env.local and fill in real values.
# .env.local is gitignored; .env.local.example is tracked.

# Google Analytics — set via siteConfig.analytics.googleAnalyticsId currently.
# The CookieConsent component reads the ID from layout.tsx prop (siteConfig.analytics.googleAnalyticsId).

# PostHog — DEFERRED for TCR per Phase 11.8. When the PostHog install happens
# post-launch, populate these and add the key-read logic back to the
# CookieConsent component (reference BCF or template).
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
